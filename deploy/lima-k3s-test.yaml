images:
  - location: https://cloud-images.ubuntu.com/releases/noble/release/ubuntu-24.04-server-cloudimg-arm64.img
    arch: aarch64
  - location: https://cloud-images.ubuntu.com/releases/noble/release/ubuntu-24.04-server-cloudimg-amd64.img
    arch: x86_64

cpus: 4
# Matches lima-k3s.yaml: Istio ambient (istiod + istio-cni + ztunnel +
# waypoint) adds ~1 GiB on top of the platform stack. On 4 GiB the keycloak
# JVM boot exceeds the 2-min keycloak-config-cli retry deadline because
# every pod is fighting for memory. Bumping here doesn't resize an existing
# VM — `mise run e2e` always nukes the prior VM first, so this takes effect
# on the next run.
memory: 8GiB
disk: 20GiB

# Skip lima's default rootless containerd + buildkit + stargz setup —
# k3s ships its own containerd and we never invoke `nerdctl` from the host.
# This shaves ~2 minutes off cold-boot provisioning.
containerd:
  system: false
  user: false

provision:
  - mode: system
    script: |
      #!/bin/sh
      set -e
      # nfs-common is required so the kubelet can mount the platform-rwx
      # PVCs served by the in-cluster NFS provisioner. Without this
      # package, agent pods fail with "bad option; … you might need a
      # /sbin/mount.<type> helper program."
      DEBIAN_FRONTEND=noninteractive apt-get update
      DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends nfs-common
      mkdir -p /etc/rancher/k3s
      cat > /etc/rancher/k3s/config.yaml << 'EOF'
      kubelet-arg:
        - "container-log-max-size=10Mi"
        - "container-log-max-files=3"
      EOF
      if [ ! -d /var/lib/rancher/k3s ]; then
        curl -sfL https://get.k3s.io | INSTALL_K3S_EXEC="server --write-kubeconfig-mode 644 --https-listen-port 16445" sh -
      fi
      mkdir -p /var/lib/rancher/k3s/server/manifests
      cat > /var/lib/rancher/k3s/server/manifests/traefik-config.yaml << 'EOF'
      apiVersion: helm.cattle.io/v1
      kind: HelmChartConfig
      metadata:
        name: traefik
        namespace: kube-system
      spec:
        valuesContent: |
          ports:
            platform:
              port: 5555
              expose:
                default: true
              exposedPort: 5555
              protocol: TCP
      EOF

probes:
  - mode: readiness
    description: k3s is ready
    script: |
      #!/bin/bash
      set -eux -o pipefail
      if ! timeout 30s bash -c "until test -f /etc/rancher/k3s/k3s.yaml; do sleep 3; done"; then
        echo >&2 "k3s is not running yet"
        exit 1
      fi

copyToHost:
  - guest: /etc/rancher/k3s/k3s.yaml
    host: "{{.Dir}}/copied-from-guest/kubeconfig.yaml"
    deleteOnStop: true

portForwards:
  - guestPort: 5555
    hostPort: 5555
  - guestPort: 16445
    hostPort: 16445

message: |
  Test k3s cluster is ready. Access services via port 5555.
  KUBECONFIG: {{.Dir}}/copied-from-guest/kubeconfig.yaml
