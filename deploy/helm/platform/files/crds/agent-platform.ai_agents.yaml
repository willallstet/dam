# Code generated by controller-gen. DO NOT EDIT.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: agents.agent-platform.ai
spec:
  group: agent-platform.ai
  names:
    kind: Agent
    listKind: AgentList
    plural: agents
    shortNames:
    - agt
    singular: agent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      type: string
    - jsonPath: .spec.image
      name: Image
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          Agent is the durable, owned, runnable resource — definition, runtime state,
          and lifecycle in one custom resource (ADR-046, ADR-058).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AgentSpec is the desired state of an Agent — the sole durable per-agent
              resource after ADR-046 collapsed Instance into Agent. The api-server is the
              sole writer.

              There is no desiredState field: running-vs-hibernated is not stored intent
              but observed status the controller derives from activity (ADR-058). Security
              context and scheduling are chart-only (config.AgentBase) and cannot be set
              here by design.
            properties:
              agentHome:
                description: |-
                  AgentHome is the resolved HOME inside the agent container. Any $HOME
                  literals in Mounts/SkillPaths are already resolved against it at write
                  time, so the controller never sees $HOME.
                type: string
              description:
                description: Description is an optional human-readable description.
                type: string
              env:
                description: Env are plain environment variables projected into the
                  agent container.
                items:
                  description: EnvVar is a plain name/value environment variable.
                  properties:
                    name:
                      type: string
                    value:
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
              grantedConnectionIds:
                description: GrantedConnectionIDs are the connection IDs granted to
                  this agent.
                items:
                  type: string
                type: array
              grantedSecretIds:
                description: |-
                  GrantedSecretIDs are the credential Secret IDs granted to this agent's
                  egress — intent written by the api-server. ADR-058 moved these from a
                  ConfigMap annotation into spec, because they are reconciled by the
                  controller into the credential set mounted on the gateway.
                items:
                  type: string
                type: array
              image:
                description: Image is the agent container image.
                type: string
              imagePullPolicy:
                description: ImagePullPolicy overrides the chart-wide default; empty
                  = inherit.
                type: string
              init:
                description: Init is an optional one-shot init script run before the
                  agent starts.
                type: string
              mounts:
                description: Mounts declares the agent's volumes; a persisted mount
                  becomes a PVC.
                items:
                  description: Mount declares a volume mounted into the agent container.
                  properties:
                    path:
                      description: Path is the absolute mount path inside the container.
                      type: string
                    persist:
                      description: |-
                        Persist marks the mount as backed by a retained PVC rather than an
                        emptyDir that dies with the pod.
                      type: boolean
                    size:
                      description: |-
                        Size is an optional K8s resource Quantity (e.g. "2Gi") for a persisted
                        mount's PVC. Empty falls back to StorageSize, then the chart default.
                        Ignored when Persist is false.
                      type: string
                  required:
                  - path
                  - persist
                  type: object
                type: array
              name:
                description: Name is an optional human-readable name.
                type: string
              resources:
                description: Resources are the agent container's resource requests
                  and limits.
                properties:
                  limits:
                    additionalProperties:
                      type: string
                    type: object
                  requests:
                    additionalProperties:
                      type: string
                    type: object
                type: object
              secretRef:
                description: |-
                  SecretRef names a K8s Secret whose keys are envFrom-projected into the
                  agent container (operator-supplied envs).
                type: string
              skillPaths:
                description: SkillPaths are absolute paths to skills installed into
                  the agent.
                items:
                  type: string
                type: array
              storageSize:
                description: StorageSize overrides the chart-wide default PVC size;
                  empty = inherit.
                type: string
            required:
            - image
            type: object
          status:
            description: |-
              AgentStatus is the observed state of an Agent. The controller is the sole
              writer, via the status subresource.
            properties:
              conditions:
                description: |-
                  Conditions are the source of truth for the agent's operational state.
                  See the Condition* constants for the well-known types.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the spec generation last reconciled.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
