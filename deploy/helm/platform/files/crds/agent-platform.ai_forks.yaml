# Code generated by controller-gen. DO NOT EDIT.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: forks.agent-platform.ai
spec:
  group: agent-platform.ai
  names:
    kind: Fork
    listKind: ForkList
    plural: forks
    singular: fork
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.agentName
      name: Agent
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Fork is a per-turn impersonation run derived from an Agent (ADR-046).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ForkSpec is the per-turn ephemeral runtime that derives from an Agent
              (ADR-046: Forks survived the Instance/Agent collapse). The parent Agent's
              egress surface scopes what the fork can reach. The api-server is the sole
              writer.
            properties:
              agentName:
                description: AgentName names the parent Agent this fork impersonates.
                type: string
              foreignSub:
                description: ForeignSub is the foreign user identity the fork runs
                  as.
                type: string
              sessionId:
                description: SessionID is the optional originating session.
                type: string
            required:
            - agentName
            - foreignSub
            type: object
          status:
            description: |-
              ForkStatus is the observed state of a fork. The controller is the sole
              writer, via the status subresource.
            properties:
              error:
                description: ForkError carries a structured failure reason on a failed
                  fork.
                properties:
                  detail:
                    type: string
                  reason:
                    type: string
                required:
                - reason
                type: object
              jobName:
                type: string
              phase:
                description: ForkPhase is the lifecycle phase of a fork run.
                enum:
                - Pending
                - Ready
                - Failed
                - Completed
                type: string
              podIP:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
